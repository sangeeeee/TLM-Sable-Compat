# 项目约定

- 用户未明确要求时，版本号固定为 `0.0.1-beta`。
- 按首次实现设计数据与配置格式，不添加旧版本配置、物品数据或世界存档的迁移兼容。结构在游戏内装配、分裂、卸载、移除时的绑定更新属于正常功能，须保留。
- 涉及 AI、坐标或 Mixin 的变更，运行适当的服务器 GameTest；涉及 MUHC 时使用可选 MUHC/Create 测试配置。不要将服务器测试描述为完整整合包客户端实测。
